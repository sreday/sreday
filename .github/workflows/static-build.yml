name: Build and Deploy
on:
  push:
    branches:
      - main
  schedule:
    - cron: '5 4 * * *'   # daily ~5AM UK (04:05 UTC) so /status/ is fresh before work
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: History for /status/ speaker log
        run: git fetch --no-tags --shallow-since="$(date -u -d '45 days ago' +%F)" origin ${{ github.ref_name }}

      - name: Deps
        run: |
          make --version
          make deps

      - name: Red flag check
        if: github.event_name == 'push'
        continue-on-error: true
        env:
          REDFLAG_URL: ${{ secrets.REDFLAG_URL }}       # "Red flag alert" Apps Script (llmday/_build/redflag-alert.gs)
          REDFLAG_TOKEN: ${{ secrets.REDFLAG_TOKEN }}
          BEFORE: ${{ github.event.before }}
          AFTER: ${{ github.sha }}
        run: python _build/redflag.py                   # "Did you just nuke <event>?" email when a push swaps a whole lineup

      - name: Build 🔧
        env:
          LUMA_API_KEYS: ${{ secrets.LUMA_API_KEYS }}   # one or more Luma calendar keys, comma-separated; /status/ registrations
        run: |
          make clean
          make generate

      - name: Optimize Images
        run: python _build/optimize_images.py

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4.4.0
        with:
          branch: gh-pages
          folder: static
